type Props = { missionTitle?: string | null };
export function MissionSection({ missionTitle }: Props) {
  return <section className="sections" id="offer"><div className="card" id="details"><h2>What this app is becoming</h2><p>{missionTitle ? `This public app is being continuously improved to serve: ${missionTitle}` : "AutoApp is waiting for a mission from Slack #general before specializing this page."}</p><ul><li>Mission-aware headline and offer copy</li><li>Clear primary and secondary calls to action</li><li>Small, reviewable improvements proposed in Slack</li></ul></div><div className="card"><h2>How AutoApp operates</h2><p>The control plane stays in Slack. AutoApp observes #general, decides on a small change, launches a Cursor cloud agent to implement it, and watches GitHub and Vercel notifications.</p><p>No admin UI is exposed on the public web app.</p></div></section>;
}
