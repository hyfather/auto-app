type Props = { missionTitle?: string | null };
export function MissionSection({ missionTitle }: Props) {
  const isSelfImproving = missionTitle && /self.?improv|perfect.*app|elegant.*ux|shadcn/i.test(missionTitle);
  const isLightLandingPage = !missionTitle || /light|dark mode|background|landing page/i.test(missionTitle);
  return <section className="sections" id="offer"><div className="card" id="details"><h2>What AutoApp offers</h2>{isLightLandingPage ? (<><p>AutoApp is for founders, operators, and product teams who want a public Next.js landing page that keeps improving from clear Slack direction. This light-background homepage explains the offer for search visitors and makes the next step obvious.</p><ul><li><strong>Mission-aware homepage copy</strong> — headline, description, and CTAs stay aligned with the active goal</li><li><strong>Human-approved implementation</strong> — each change is proposed, reviewed, and shipped as a focused PR</li><li><strong>Visitor-ready presentation</strong> — a bright, responsive sales page without exposing private admin controls</li></ul></>) : isSelfImproving ? (<><p>A beautiful, production-ready web app that refines itself based on your direction. Powered by shadcn components for a polished, accessible interface.</p><ul><li><strong>Slack as your command center</strong> — propose features, review changes, and approve PRs without leaving your workspace</li><li><strong>Vercel-hosted for your users</strong> — instant deploys, global edge performance, and zero DevOps overhead</li><li><strong>Incremental, human-approved changes</strong> — every improvement is a reviewable PR, never a black-box update</li></ul></>) : (<><p>This public app is being continuously improved to serve: {missionTitle}</p><ul><li>Mission-aware headline and offer copy</li><li>Clear primary and secondary calls to action</li><li>Small, reviewable improvements proposed in Slack</li></ul></>)}</div><div className="card"><h2>Why teams trust AutoApp</h2><p>The control plane lives in Slack—where your team already works. AutoApp proposes changes, you approve them, and the result deploys to Vercel automatically.</p><ul><li><strong>Transparent process</strong> — see every proposed change before it ships</li><li><strong>No admin UI exposed</strong> — end users see only the polished public app</li><li><strong>Built on open standards</strong> — Next.js, Tailwind CSS, shadcn/ui, and Vercel</li></ul></div></section>;
}
